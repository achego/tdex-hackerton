import 'package:flutter/material.dart';

class AppConstatnts {
  static const defPadding = 16.0;

  /// in seconds
  static const lockScreenTime = 120;
  static const double buttonHeight = 53;
  static const double defBorderRadius = 10;

  static BorderRadius bordeRadius = BorderRadius.circular(defBorderRadius);
}

const kTransparent = Colors.transparent;
const kDefPadding = AppConstatnts.defPadding;
const kDefBorderRadius = AppConstatnts.defBorderRadius;
const kButtonHeight = AppConstatnts.buttonHeight;
const kDefPaddingX = EdgeInsets.symmetric(horizontal: AppConstatnts.defPadding);
